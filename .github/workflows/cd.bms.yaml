name: CD BMS

on:
  workflow_dispatch

jobs:

  cd:

    runs-on: ubuntu-latest
    
    steps:
      - name: Crear carpeta .ssh
        run: |
          mkdir ~/.ssh
      - name: Mostrar contendido
        run: ls la
