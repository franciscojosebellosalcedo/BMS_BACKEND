name: Mostrar poyecto

on:
  workflow_dispatch:
jobs:
  
  backend:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5

      - run: |
          cat <<EOF> .env.prod
          APP_NAME=$APP_NAME
          APP_HOST=$APP_HOST
          APP_PORT=$APP_PORT
          EOF
      - run: ls
      - run: cat .env.prod


      

