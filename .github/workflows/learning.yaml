name: Primer workflow

on:
  workflow_dispatch:
    inputs:
      name: 
        required: true
        default: "Francisco"
        type: string

jobs:
  
  saludar:
    runs-on: ubuntu-latest
    
    steps:
      - name: saludar
        run: echo "Hola ${{inputs.name}}"

